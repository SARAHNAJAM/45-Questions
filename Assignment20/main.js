"use strict";
// Assignment 20
// two objects
const vegetable = {
    name: "Tamato",
    price: 100
};
const fruits = {
    name: "Apple",
    price: 200
};
console.log(`vegetable name: ${vegetable.name} vegetable price: ${vegetable.price}`);
console.log(`fruit name: ${fruits.name} friut price: ${fruits.price}`);
