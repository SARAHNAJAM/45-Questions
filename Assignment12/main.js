"use strict";
// Assignment12
let transpotation = [`Airplane`, `Train`, `car`, `motorcycle`];
for (let i = 0; i < transpotation.length; i++) {
    console.log(`i would like to own ` + transpotation[i]);
}
;
